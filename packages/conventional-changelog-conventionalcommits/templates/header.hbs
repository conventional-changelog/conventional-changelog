## {{#if isPatch~}}
<small>
{{~/if}}{{#if @root.linkCompare~}}
  [{{version}}]({{compareUrlFormat}})
{{~else}}
  {{~version}}
{{~/if}}
{{~#if title}} "{{title}}"
{{~/if}}
{{~#if date}} ({{date}})
{{~/if}}{{#if isPatch~}}
  </small>
{{/if}}
